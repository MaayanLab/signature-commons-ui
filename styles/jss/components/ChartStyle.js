import {
  card,
  main,
  white,
} from '../theme.js'

export const cardChartStyle = (theme) => ({
  cardChart: {
    background: white,
  },
  progress: {
    margin: '90px auto 90px auto',
  },
  card,
  main,
})
