import {
  card,
  main,
  white,
} from "../theme.js";

export const cardChartStyle = theme => ({
  cardChart: {
    maxHeight: 300
  },
  progress: {
    margin: '90px auto 90px auto'
  },
  card,
  main
});