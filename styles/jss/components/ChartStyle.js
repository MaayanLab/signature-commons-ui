import {
  card,
  main,
  white,
} from "../theme.js";

export const cardChartStyle = theme => ({
  cardChart: {
    background: '#fff',
  },
  progress: {
    margin: '90px auto 90px auto'
  },
  card,
  main
});